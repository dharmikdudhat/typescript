//typescript catches error at compile time instead of runtime which makes debugging easier and reduces the likelihood of bugs in production

// typescript is superset of javascript

// browser do not compile typescript so we need to compile the ts into js 

 